"use strict" ;   //treat all JS code as newer version

let name="Treesha" //string in double quotes
let age = 20 // number =>2 to power53 no quotes needed
let isLoggedIn = false //bool- true and false
// null - standalone value, representation of empty value, type - object (also a error of the language.)
// undefined - value isnt assigned yet, type - undefined.
// symbol - used for uniqueness.
// object - 

// Primitive datatypes(call by value) - seven - string, number, boolean, null, undefined, BigInt, Symbol

// Non Primitive (reference) - arrays, objects, functions

// *********************************MEMORY***********************

// stack (Primitive), Heap(non-primitive)